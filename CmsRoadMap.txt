نقشه راه جامع پروژه AnosheCMS
۱. معماری و اصول فنی (Architecture & Principles)
سبک معماری: Clean Architecture (پیازی/Onion)

Domain: هسته مرکزی، بدون وابستگی خارجی (Pure Entities).

Application: لایه سرویس‌ها، اینترفیس‌ها و DTOها.

Infrastructure: پیاده‌سازی دیتابیس (EF Core)، فایل سیستم و سرویس‌های جانبی.

API: کنترلرها و نقطه ورود (End-points).

الگوهای طراحی:

Result Pattern: بازگرداندن اشیاء ساختاریافته (Success/Fail) بجای پرتاب Exception.

Dependency Injection (DI): استفاده در تمام سطوح.

قوانین دیتابیس:

Code-First با Migrations.

استفاده از Fluent API برای کانفیگ (جدا از کلاس‌های Entity).

Soft Delete سراسری (Global Query Filter).

Auditing خودکار (ثبت تغییرات در SaveChanges).

۲. تکنولوژی‌ها (Tech Stack)
Backend: ASP.NET Core 8 Web API

Database: SQL Server (Entity Framework Core)

Frontend: Vue.js 3 + Vite + Pinia (Setup Stores)

UI: Custom CSS + VueDraggable (برای فرم‌ساز)

Authentication: JWT (Access Token + Refresh Token)

۳. وضعیت فعلی: ماژول‌های تکمیل شده (Completed Modules)
✅ ماژول امنیت و احراز هویت (Core Security)
سیستم لاگین و مدیریت توکن (JWT + Refresh Token).

سیستم RBAC (کنترل دسترسی بر اساس نقش و پرمیشن‌های ریز).

دایرکتیو v-can در فرانت‌اند برای مخفی کردن دکمه‌ها.

متد نجات (Rescue Admin) برای بازیابی دسترسی مدیر در مواقع اضطراری.

✅ ماژول کاربران و نقش‌ها (Users & Roles)
مدیریت کامل کاربران (CRUD).

مدیریت نقش‌ها و تخصیص دسترسی‌ها (ماتریس چک‌باکس‌ها).

✅ ماژول فرم‌ساز سازمانی (Form Builder - Phase 3)
Backend:

پشتیبانی از انواع فیلد (Text, Number, Select, Radio, Checkbox, Date).

ذخیره‌سازی داینامیک و جلوگیری از خطاهای JSON Loop.

کنترلر عمومی (PublicFormController) برای ثبت پاسخ‌ها بدون لاگین.

Frontend:

طراحی ویژوال با Drag & Drop.

پنل تنظیمات فیلد (ویرایش لیبل، اجباری بودن، و گزینه‌ها).

موتور نمایش عمومی (/view/:slug) برای رندر کردن فرم نهایی.

مشاهده جدول ورودی‌ها (Data Grid) و خروجی اکسل (CSV).

۴. کارهای باقی‌مانده (Pending Tasks & Next Steps)
🚧 فاز ۴: سیستم نظارت و لاگ‌گیری (Audit Logging) - (اولویت فعلی)
وضعیت: زیرساخت دیتابیس (AuditLogs و Interceptor) آماده و فعال است.

کار باقی‌مانده: ساخت رابط کاربری (UI) در پنل ادمین برای مشاهده لاگ‌ها.

ایجاد AuditLogService و AdminAuditController.

طراحی صفحه AuditIndex.vue (جدول نمایش تغییرات، کاربر، زمان، IP).

نمایش تفاوت مقادیر (OldValues vs NewValues) به صورت JSON خوانا.

🔮 فاز ۵: مدیریت محتوا (Headless CMS Core)
تعریف انواع محتوا (Content Types): سیستمی شبیه به فرم‌ساز اما برای ساختار مقالات، محصولات و...

مدیریت محتوا (Content Entries): فرم‌های تولید محتوا بر اساس Typeهای تعریف شده.

API خروجی: ارائه محتوا به صورت JSON برای فرانت‌اند اصلی سایت.

🎨 فاز ۶: بازسازی رابط کاربری (UI Refactoring)
مهاجرت از CSSهای دستی به یک فریم‌ورک UI منسجم (مانند PrimeVue یا Tailwind).

بهبود تجربه کاربری (UX) و انیمیشن‌ها.