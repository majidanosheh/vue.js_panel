سلام. به عنوان Lead Architect، وضعیت دقیق و فنی ماژول‌های AnosheCMS را بر اساس کدهای موجود در فایل‌های آپلود شده و کارهایی که با هم انجام دادیم، به شرح زیر گزارش می‌دهم:

🔐 ۱. ماژول احراز هویت و امنیت (Authentication & Security)
این بخش هسته مرکزی سیستم است که امنیت API و دسترسی‌ها را تامین می‌کند.

✅ لاگین استاندارد (Standard Login): ورود با ایمیل و رمز عبور، تولید توکن JWT و Refresh Token.

✅ فراموشی رمز عبور (Forgot Password): کنترلر و سرویس برای درخواست لینک بازیابی و ریست کردن رمز وجود دارد.

✅ تایید ایمیل (Email Verification): سیستم ارسال لینک فعال‌سازی و وریفای کردن ایمیل پیاده‌سازی شده است.

✅ مدیریت نشست‌ها (Session Management): ثبت دستگاه، مرورگر و IP کاربر هنگام ورود و امکان ابطال توکن.

✅ حالت نجات (Rescue Mode): یک بک‌دور امن برای بازیابی دسترسی ادمین در صورت قفل شدن سیستم.

❌ SSO (Single Sign-On): پیاده‌سازی نشده است.

❌ لاگین با شبکه‌های اجتماعی (Social/Google Login): در Startup.cs و کنترلرها کدی برای OAuth/Google وجود ندارد.

❌ احراز هویت دو مرحله‌ای (2FA): فیلد آن در دیتابیس هست (TwoFactorEnabled)، اما لاجیک ارسال کد پیامک/ایمیل هنوز فعال نیست.

👥 ۲. ماژول مدیریت کاربران (User Management)
✅ عملیات CRUD: ایجاد، ویرایش، لیست‌گیری و حذف کاربران.

✅ حذف نرم (Soft Delete): کاربران از دیتابیس پاک نمی‌شوند، بلکه پرچم IsDeleted می‌خورند و امکان بازیابی دارند.

✅ تخصیص نقش: امکان دادن یک یا چند نقش به کاربر.

❌ پروفایل کاربری: امکان آپلود عکس پروفایل در دیتابیس هست (ProfilePictureUrl) اما API آپلود آن در UserService تکمیل نشده است.

🛡️ ۳. ماژول نقش‌ها و دسترسی‌ها (RBAC)
✅ نقش‌های سیستمی: نقش SuperAdmin به صورت حفاظت شده وجود دارد.

✅ دسترسی‌های ریز (Granular Permissions): کنترل دسترسی بر اساس نام ماژول و اکشن (مثلاً Permissions.Forms.Create).

✅ ماتریس دسترسی: امکان تیک زدن دسترسی‌ها برای هر نقش به صورت داینامیک.

✅ دایرکتیو فرانت: دکمه‌ها در فرانت‌اند بر اساس دسترسی مخفی/نمایان می‌شوند (با v-can).

📝 ۴. ماژول فرم‌ساز سازمانی (Form Builder) - (کامل‌ترین ماژول)
✅ طراحی ویژوال: قابلیت Drag & Drop برای چیدن فیلدها.

✅ انواع فیلد: متن، متن طولانی، عدد، تاریخ، چک‌باکس، رادیو باتن، لیست کشویی (Select).

✅ مدیریت گزینه‌ها: امکان تعریف گزینه‌ها برای لیست‌های کشویی و رادیویی (ذخیره به صورت خط‌به‌خط).

✅ نمایش عمومی (Public View): موتور رندرینگ برای نمایش فرم به کاربران عادی بدون نیاز به لاگین.

✅ ثبت ورودی‌ها (Submissions): دریافت و اعتبارسنجی پاسخ‌ها و ذخیره در دیتابیس.

✅ مشاهده داده‌ها (Data Grid): جدول داینامیک در پنل ادمین برای دیدن پاسخ‌ها.

✅ خروجی اکسل: امکان دانلود پاسخ‌ها به فرمت CSV.

⚠️ منطق شرطی (Conditional Logic): زیرساخت بک‌اند (ConditionalLogicEvaluator) آماده است، اما در رابط کاربری (UI) هنوز ابزاری برای تعریف شرط ("اگر X آنگاه Y") نداریم.

📰 ۵. ماژول مدیریت محتوا (Headless CMS Core)
✅ ساخت نوع محتوا (Content Types): امکان تعریف ساختار دیتابیس به صورت داینامیک (مثلاً تعریف نوع "مقاله" با فیلدهای تایتل و بدنه).

✅ مدیریت محتوا (Content Entries): فرم‌های تولید محتوا بر اساس Typeهای تعریف شده.

✅ API عمومی (Headless): خروجی JSON محتوا برای استفاده در فرانت‌‌اند سایت (api/content/{slug}).

✅ ذخیره‌سازی JSON: داده‌های منعطف به صورت JSON در SQL Server ذخیره می‌شوند.

🖼️ ۶. ماژول رسانه (Media Manager)
✅ آپلود فایل: ذخیره فایل‌ها در پوشه wwwroot/uploads و ثبت اطلاعات در دیتابیس.

✅ لیست گالری: مشاهده و حذف فایل‌ها.

❌ مدیریت پیشرفته: قابلیت‌هایی مثل کراپ کردن عکس، پوشه‌بندی تو در تو، یا اتصال به فضای ابری (S3) فعلاً وجود ندارد.

📜 ۷. ماژول لاگ و نظارت (Audit & Monitoring)
✅ لاگ دیتابیس (System Audit): اینترسپتور SaveChanges فعال است و تمام تغییرات (Create/Update/SoftDelete) را به همراه مقادیر قدیم و جدید در جدول AuditLogs ذخیره می‌کند.

✅ تاریخچه ورود: لاگ‌های ورود و خروج کاربران با جزئیات IP و مرورگر ذخیره می‌شود.

❌ رابط کاربری (UI): هنوز صفحه‌ای در پنل ادمین برای مشاهده این لاگ‌ها نساخته‌ایم (این برنامه بعدی ماست).

📋 خلاصه مدیریتی
پروژه در وضعیت "Enterprise Core Ready" قرار دارد. زیرساخت‌های پیچیده (فرم‌ساز، امنیت، محتوای داینامیک) تکمیل شده‌اند. کمبودهای فعلی (SSO, Google Login, 2FA) فیچرهای جانبی هستند که می‌توانند در فازهای بعدی (Phase 5+) اضافه شوند، اما مانع عملکرد اصلی سیستم نیستند.